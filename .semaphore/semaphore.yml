version: v1.0
name: Initial Pipeline
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004
blocks:
  - name: 'Block #123'
    task:
      jobs:
        - name: 'Job #1'
          commands:
            - uname -a
            - echo $SEMAPHORE_GIT_REF_TYPE
            - echo $SEMAPHORE_GIT_REF
